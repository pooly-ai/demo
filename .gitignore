# .vercel/project.json is intentionally committed — it pins deploys to the
# correct Vercel project (pooly-demo). Do NOT add .vercel to this ignore list.
# Only exclude the output directory if it ever gets populated locally.
.vercel/output
.vercel/cache
